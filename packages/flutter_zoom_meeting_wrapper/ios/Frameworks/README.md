Place Zoom's iOS Meeting SDK frameworks and resources here.

Required files come from the same Zoom iOS Meeting SDK download, including
`MobileRTC.xcframework`, companion Zoom frameworks such as `zoomcml`, and
`MobileRTCResources.bundle`. Keep binaries out of git, then run `pod install`
from `ios/`.
