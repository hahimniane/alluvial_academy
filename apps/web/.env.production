# `next build` loads this file, so every shipped bundle points at the live
# project. Without it the selection in src/lib/firebase.ts falls through to the
# dev config, and the public site's enrollment and contact forms write to
# alluwal-dev where nobody reads them.
NEXT_PUBLIC_FIREBASE_ENV=prod
