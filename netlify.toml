[build]
command = "flutter build web --release --pwa-strategy=none"
publish = "build/web"

[build.environment]
FLUTTER_VERSION = "stable"

# SPA routing for Flutter web
[[redirects]]
from = "/*"
to = "/index.html"
status = 200 
