/// Task priority levels
enum TaskPriority { low, medium, high }

/// Task status
enum TaskStatus { todo, inProgress, done }

/// Recurrence type for tasks (kept for backward compatibility)
enum RecurrenceType { none, daily, weekly, monthly }
