rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Allow all operations for authenticated users - no restrictions
    match /{document=**} {
      allow read, write: if true; // Completely permissive
    }
  }
} 